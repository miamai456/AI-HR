# AIHR 数据分析师方向优化说明

## 优化目标

本轮优化把 AIHR 从“指标看板集合”升级为“数据分析师作品集型项目”。优化重点不是单纯增加图表数量，而是让面试官能快速理解：

- 这个招聘推荐系统在解决什么业务问题。
- AI 推荐相对人工推荐是否有优势。
- 优势或风险出现在哪些岗位、地区、模型版本或招聘团队。
- 数据质量、模型漂移和样本成熟度是否足以支撑结论。
- 项目中体现了哪些数据分析、准实验评估和机器学习解释能力。

## 第一阶段：面试官 30 秒能看懂的分析入口

### 0. 导航栏中文化

修改文件：

- `app/Home.py` 重命名为 `app/首页.py`
- `app/pages/1_Funnel.py` 重命名为 `app/pages/1_招聘漏斗.py`
- `app/pages/2_Trend.py` 重命名为 `app/pages/2_趋势分析.py`
- `app/pages/3_Effectiveness.py` 重命名为 `app/pages/3_效果评估.py`
- `app/pages/4_Monitoring.py` 重命名为 `app/pages/4_模型监控.py`
- `app/pages/5_Data_Quality.py` 重命名为 `app/pages/5_数据质量.py`

同步修改：

- `README.md`
- `AGENTS.md`
- `scripts/start-dev.ps1`
- `docs/实施计划.md`

优化原因：

Streamlit 默认导航栏来自入口文件和 `app/pages` 目录下的文件名。原来的 `Home / Funnel / Trend / Effectiveness / Monitoring / Data Quality` 会让项目显得中英文混用、不够完整。本轮将导航入口统一中文化，使面试官看到的页面结构更像完整中文分析系统。

### 1. 首页升级为 Executive Dashboard

修改文件：`app/首页.py`

新增内容：

- 将首页从普通总览改为“AIHR 招聘推荐分析驾驶舱”。
- 同时调用 overview、effectiveness、monitoring、data-quality、prediction-insights 五类接口。
- 新增核心结论指标：
  - 推荐量
  - AI 占比
  - 面试率
  - 调整后 AI 差异
  - 结论可信度
- 新增自动分析结论卡片，直接用业务语言解释：
  - AI 推荐与人工推荐谁表现更好。
  - 最近一期哪个来源面试率最高。
  - 当前数据质量和模型漂移是否影响结论可信度。
  - 当前最值得放大的分群机会。
- 新增首页业务漏斗速览。
- 新增机会与风险区域：
  - 分群机会 Top 列表。
  - 推荐来源监控状态表。
- 保留未解决告警展示。

体现能力：

- Executive Summary 写作能力。
- 多接口数据整合能力。
- 从指标到业务建议的分析表达能力。
- 对“结论可信度”的风险意识。

### 2. 公共 UI 增强

修改文件：`app/ui.py`

新增内容：

- 新增 `format_pp()`，统一展示百分点差异。
- 新增 `render_insight_box()`，用于每个页面展示自动分析结论。
- 新增风险等级标签和颜色映射。
- 保留并强化“确定分析范围”后的跨页面筛选状态保持能力。

体现能力：

- 统一指标展示口径。
- 降低页面重复代码。
- 让每个分析页都有一致的“结论先行”表达方式。

## 第二阶段：数据分析师核心分析能力强化

### 3. 招聘漏斗页增强

修改文件：`app/pages/1_招聘漏斗.py`

新增内容：

- 保留 AI 与人工推荐漏斗对比。
- 新增阶段转化率图。
- 新增阶段损耗率图。
- 新增漏斗自动分析结论：
  - 最大损耗发生在哪个来源、哪个环节。
  - AI 相对人工优势最大的环节。
- 新增阶段流入人数、流出人数 hover 信息。

体现能力：

- 漏斗分析。
- 转化率和损耗率拆解。
- 定位业务瓶颈。

### 4. 12 个月趋势页增强

修改文件：`app/pages/2_趋势分析.py`

新增内容：

- 新增 3 期滚动面试率，降低单月波动干扰。
- 新增面试率环比变化。
- 新增推荐量趋势。
- 新增入职率趋势。
- 新增趋势明细表。
- 新增趋势自动分析结论：
  - 最近一期哪个来源表现最好。
  - AI 与人工最近一期差异。
  - 波动最大的月份和来源。

体现能力：

- 时间序列趋势分析。
- 环比变化分析。
- 用滚动均值识别稳定趋势。
- 同时关注过程指标和最终指标。

### 5. 效果评估页增强

修改文件：`app/pages/3_效果评估.py`

新增内容：

- 重构为准实验分析报告式页面。
- 新增核心指标：
  - AI 原始面试率
  - 人工原始面试率
  - 原始差异
  - 倾向得分调整后差异
  - 共同支持样本保留率
- 新增效果评估自动结论。
- 新增原始面试率 vs 调整后面试率对比图。
- 新增共同支持诊断表。
- 新增协变量平衡 SMD 图。
- 新增方法诊断说明。

体现能力：

- 观察性数据下的严谨效果评估。
- 倾向得分调整理解。
- 共同支持和极端权重处理。
- 协变量平衡诊断。
- 明确区分关联分析和因果结论。

### 6. 数据质量页增强

修改文件：`app/pages/5_数据质量.py`

新增内容：

- 将页面定位从“检查列表”升级为“分析可信度判断”。
- 新增数据质量自动结论：
  - 当前是否可信。
  - 检查覆盖样本数。
  - 受影响记录数。
  - 是否需要先修复再解释模型效果。
- 新增数据层级记录数图。
- 新增质量检查状态分布图。
- 新增影响范围 Top 检查项图。

体现能力：

- 数据质量意识。
- 数据血缘和层级理解。
- 在结论前先判断数据是否可靠。

## 第三阶段：机器学习解释和模型监控能力强化

### 7. 机器学习洞察页增强

修改文件：`app/pages/6_机器学习洞察.py`

已有并增强内容：

- 模型判别力：AUC、Accuracy、目标转化率。
- 概率校准：预测概率分层 vs 实际面试率。
- 层级提升：不同概率段相对平均面试率的 lift。
- 特征解释：方向化特征重要性。
- 分群分析：预测面试率 vs 实际面试率。
- 异常检测：Isolation Forest 异常样本复核。

本轮新增内容：

- 新增推荐阈值模拟：
  - 优先处理高概率推荐时的累计推荐量。
  - 累计预测面试率。
  - 累计实际面试率。
- 新增阈值策略表，辅助回答“如果业务资源有限，应该优先处理哪部分推荐”。

体现能力：

- Logistic Regression 转化预测。
- 模型校准理解。
- 特征贡献解释。
- 异常检测。
- 阈值策略和资源分配分析。

### 8. 模型监控页增强

修改文件：`app/pages/4_模型监控.py`

新增内容：

- 新增高风险漂移、中等漂移、最大面试率变化、诊断结论数量指标。
- 新增监控自动分析结论：
  - 哪个推荐来源变化最大。
  - 漂移风险是否影响结论可信度。
  - 首要诊断结论是什么。
- 新增模型版本趋势标签页：
  - 模型版本推荐量。
  - 模型版本面试率。
- 新增漂移诊断图：
  - 按特征展示漂移值。
  - 按严重度着色。
- 新增告警结论表。

体现能力：

- 模型监控。
- 数据漂移诊断。
- 模型版本效果追踪。
- 风险分级和业务解释。

### 9. 新增指标字典页

新增文件：`app/pages/7_指标字典.py`

新增内容：

- 招聘业务指标说明：
  - 推荐量
  - 联系率
  - 回复率
  - 面试率
  - Offer 率
  - 入职率
  - AI 占比
- 建模与监控指标说明：
  - AUC
  - Accuracy
  - Lift
  - 校准差
  - SMD
  - PSI
  - JSD
- 每个指标都包含公式、业务含义、注意事项。
- 明确说明当前项目默认基于观察性数据，差异应解释为关联分析，不直接解释为因果效果。

体现能力：

- 指标体系建设。
- 指标口径文档化。
- 面向非项目背景面试官的解释能力。

## 页面优化后的项目结构

- `首页.py`：Executive Dashboard，负责快速说明项目结论和业务价值。
- `1_招聘漏斗.py`：招聘漏斗分析，负责定位转化瓶颈。
- `2_趋势分析.py`：趋势分析，负责判断效果是否稳定。
- `3_效果评估.py`：效果评估，负责展示准实验分析严谨性。
- `4_模型监控.py`：模型监控，负责判断模型和数据分布是否稳定。
- `5_数据质量.py`：数据质量，负责判断分析结论是否可信。
- `6_机器学习洞察.py`：机器学习解释，负责展示预测、校准、特征、异常和阈值策略。
- `7_指标字典.py`：指标口径说明，负责降低面试官理解成本。

## 后续可扩展：AI 分析助手

### 是否有必要接入 DeepSeek、Kimi 等大模型

有必要，但建议作为“分析解释层”和“交互问答层”接入，而不是替代现有统计指标和模型计算。

推荐原因：

- 当前看板已经能计算指标、模型效果、漂移和异常，但自动结论主要是规则模板。
- 大模型可以把多页指标综合成更自然的业务解释，适合面试官快速理解。
- 当出现异常、漂移、转化下降时，大模型可以帮助生成“可能原因、排查路径、下一步行动”。
- 聊天窗口可以让面试官直接问：“为什么 AI 推荐在技术岗更好？”、“这个模型漂移严重吗？”、“我应该优先看哪个分群？”

### 建议接入方式

建议在每个数据分析页面内嵌“AI 分析助手”聊天框，架构如下：

1. 每个前端页面把当前已加载的数据整理为页面上下文：
   - 首页传入 overview、effectiveness、monitoring、data-quality、prediction-insights。
   - 漏斗页传入 funnel rows 和阶段转化/损耗率。
   - 趋势页传入 12 个月趋势、环比和滚动均值。
   - 效果评估页传入倾向得分调整、共同支持和 SMD。
   - 监控页传入模型版本趋势、漂移指标和诊断结论。
   - 数据质量页传入质量检查、层级记录数和影响范围。
   - 机器学习洞察页传入概率分层、特征贡献、分群表现、异常样本和阈值模拟。
2. `render_ai_assistant()` 在每页渲染明显的聊天面板：
   - 标题使用机器人标识。
   - 聊天消息使用机器人头像。
   - 提供 3 个和本页相关的快捷问题。
   - 支持用户围绕当前页面继续追问。
3. `analysis_assistant.py` 统一决定回答方式：
   - 未配置大模型时，使用本地规则分析，保证项目可演示。
   - 配置 DeepSeek、Kimi 或其他 OpenAI-compatible API 后，切换为真实大模型问答。
   - 回答结构化输出：事实、推断、建议、结论限制。

### 推荐提示词策略

AI 助手不应自由编造结论，必须只基于系统传入的数据回答。提示词应要求：

- 只引用上下文中存在的指标。
- 明确区分“事实、推断、建议”。
- 不能把观察性差异说成因果效果。
- 如果数据质量失败或样本不足，必须先提醒结论限制。
- 回答必须面向招聘业务，不使用空泛术语。

### DeepSeek / Kimi 接入建议

DeepSeek 和 Kimi 都可以通过环境变量配置：

- `AIHR_ASSISTANT_PROVIDER=deepseek`
- `AIHR_ASSISTANT_API_KEY=...`
- `AIHR_ASSISTANT_BASE_URL=...`
- `AIHR_ASSISTANT_MODEL=...`

为了避免项目被某一个模型厂商绑定，建议后端封装统一的 `AssistantClient`，只让 provider 配置决定调用 DeepSeek、Kimi 或其他兼容服务。

### MVP 优先级

第一版建议先做：

- 在每个核心数据页嵌入“AI 分析助手”聊天框。
- 每页只传入和本页图表相关的上下文，避免回答泛化。
- 提供 3 个快捷问题：
  - “请总结当前筛选范围下的核心结论。”
  - “有哪些异常或风险需要优先处理？”
  - “如果我是面试官，应该关注哪些分析亮点？”
- 支持自由追问。

这会让项目从静态看板进一步升级为智能分析系统，也更贴近数据分析师未来的工作方式：不仅会做指标和模型，也会把分析能力产品化。

本轮已落地第一版：

- 在 `app/ui.py` 中新增 `render_ai_assistant()`，把 AI 辅助解读封装成可复用页面组件。
- 新增 `app/analysis_assistant.py`，统一封装本地规则分析和 OpenAI-compatible 大模型调用。
- 首页、招聘漏斗、趋势分析、效果评估、模型监控、数据质量、机器学习洞察、指标字典均已接入页面内嵌 AI 聊天助手。
- 不再保留单独的 AI 分析助手页面，避免导航复杂化。
- 未配置 API Key 时自动使用本地规则分析，保证项目可演示。
- 配置 `AIHR_ASSISTANT_API_KEY`、`AIHR_ASSISTANT_BASE_URL`、`AIHR_ASSISTANT_MODEL` 后，可以接入 DeepSeek、Kimi 或其他兼容服务。

每页 AI 辅助解读的作用：

- 自动基于当前页面已经加载的数据生成事实、推断和建议。
- 为初次接触看板的人提供“怎么看这个页面”的解释。
- 对异常、风险、漂移、数据质量失败等情况提供排查方向。
- 让面试官可以围绕当前图表继续追问，而不是只能被动看图。

## 面试表达建议

可以这样介绍项目：

> 这个项目不是单纯做招聘看板，而是围绕 AI 推荐是否提升招聘效率建立了一套分析闭环。我先用首页给出业务结论，再用漏斗和趋势定位问题，用倾向得分调整做观察性效果评估，用数据质量和模型监控判断结论是否可信，最后用机器学习洞察解释模型为什么这样判断，以及哪些异常样本需要人工复核。

这套表达能突出三个数据分析师能力：

- 能把复杂数据讲成业务结论。
- 能用统计和准实验方法避免草率下结论。
- 能理解机器学习模型在业务系统中的效果、风险和监控方式。
