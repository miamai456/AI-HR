# AI 招聘推荐效果评估与模型监控系统需求说明书

## 1. 项目概述

### 1.1 项目名称

AI 招聘推荐效果评估与模型监控系统（AI Recruitment Effectiveness & Monitoring System）

### 1.2 项目背景

招聘团队引入 AI 推荐后，通常只能看到推荐数量或总体转化率，无法可靠回答以下问题：

- AI 推荐是否真的优于人工推荐？
- AI 和人工面对的岗位、候选人是否具有可比性？
- AI 在哪些岗位、地区和候选人群体中效果较好或较差？
- 转化率下降来自模型、数据、招聘顾问操作还是后续流程？
- 招聘结果存在时间滞后时，当前数据是否已经成熟到可以评价？

本项目建设一套从招聘事件数据到效果评估、异常监控和业务解释的分析系统，重点体现 SQL、Python、统计分析、可视化和数据产品能力。

### 1.3 项目定位

本项目是面向数据分析师求职展示的个人作品集，不建设真实招聘交易平台，不处理真实个人隐私数据。所有候选人和招聘流程数据均由可复现的数据生成器生成，生成逻辑中包含已知业务场景，用于验证分析方法能否发现问题。

### 1.4 核心价值

- 建立统一、可追溯的招聘推荐指标口径。
- 比较 AI 推荐与人工推荐的原始效果和调整后效果。
- 识别数据漂移、效果下降和招聘漏斗异常。
- 将异常定位到岗位、地区、模型版本或顾问团队。
- 通过可交互 Dashboard 帮助业务人员形成决策。

## 2. 项目目标与非目标

### 2.1 项目目标

1. 生成不少于 100,000 条推荐记录和完整招聘漏斗事件。
2. 建立“推荐曝光 -> 顾问联系 -> 候选人回复 -> 面试 -> Offer -> 入职”的指标体系。
3. 支持 AI 推荐与人工推荐的分群、队列和调整后效果比较。
4. 支持候选人特征、模型分数和业务转化率的漂移监控。
5. 支持异常下钻和初步原因分类。
6. 提供可一键启动、可重复生成数据、可运行测试的 GitHub 项目。

### 2.2 非目标

- 不实现简历解析、职位发布、候选人聊天等完整 ATS 功能。
- 不训练大型推荐模型，只模拟推荐分数和模型版本。
- 不使用真实姓名、电话、邮箱、身份证等个人信息。
- 不把非随机观察数据包装成严格因果结论。
- MVP 不实现登录权限、Redis、消息队列和微服务治理。

## 3. 目标用户与使用场景

### 3.1 招聘业务负责人

关注 AI 推荐是否提升有效面试和入职、哪些团队需要改进，以及是否应该继续扩大 AI 使用范围。

### 3.2 招聘数据分析师

负责维护指标、评估效果、发现异常、分析人群差异并输出业务建议。

### 3.3 推荐模型负责人

关注不同模型版本的流量、特征分布、推荐分数和后续转化表现。

### 3.4 典型使用场景

1. 月度复盘：比较 AI 和人工推荐在成熟队列中的面试率与入职率。
2. 版本评估：判断新模型版本上线后是否在特定岗位发生效果下降。
3. 异常诊断：发现某地区回复率下降，并区分候选人变化和顾问联系延迟。
4. 数据监控：识别特征分布、事件完整性或数据延迟异常。

## 4. 核心业务流程

```text
职位创建
  -> 生成候选人推荐
  -> 推荐分配给招聘顾问
  -> 顾问联系候选人
  -> 候选人回复
  -> 安排并完成面试
  -> 发放 Offer
  -> 候选人入职
```

推荐来源分为：

- `ai`：由推荐模型产生，并记录模型版本、推荐分数和排名。
- `human`：由招聘顾问人工搜索或推荐。

每个后续事件必须关联唯一 `recommendation_id`，事件时间不得早于推荐时间。

## 5. 指标体系

### 5.1 核心主指标

主指标为推荐后 30 天合格面试率：

```text
30天合格面试率 = 推荐后30天内完成合格面试的人数 / 推荐人数
```

选择该指标的原因：反馈速度快于入职率，又比回复率更接近招聘质量。

### 5.2 漏斗指标

| 指标 | 计算口径 |
|---|---|
| 联系率 | 已联系推荐数 / 推荐数 |
| 回复率 | 已回复推荐数 / 已联系推荐数 |
| 面试率 | 已面试推荐数 / 已回复推荐数 |
| Offer 率 | 获得 Offer 推荐数 / 已面试推荐数 |
| 入职率 | 已入职推荐数 / 获得 Offer 推荐数 |
| 累计面试率 | 已面试推荐数 / 推荐数 |
| 累计入职率 | 已入职推荐数 / 推荐数 |

阶段转化率和累计转化率必须同时展示，避免仅优化局部环节。

### 5.3 效率指标

- 推荐到首次联系时长。
- 联系到回复时长。
- 回复到面试时长。
- 面试到 Offer 时长。
- Offer 到入职时长。
- 总招聘周期（Time to Hire）。

### 5.4 AI 使用指标

- AI 推荐占比。
- 顾问采纳率。
- 不同模型版本推荐量。
- 推荐分数分布。
- Top-K 推荐转化率。

### 5.5 统计口径规则

- 所有比率必须同时返回分子、分母和比率。
- 比率比较必须提供置信区间，不能只展示百分比差。
- 主效果只使用已经满足 30 天观察窗口的成熟推荐。
- 月度入职率只使用达到设定成熟窗口的队列，默认 90 天。
- 同一推荐在同一漏斗阶段只计数一次。
- Dashboard 默认使用事件发生时间所在时区 `Asia/Shanghai`。

## 6. 数据设计

### 6.1 数据规模

- 时间跨度：12 个月。
- 推荐记录：100,000 至 300,000 条。
- 岗位数量：1,000 至 3,000 个。
- 候选人数量：50,000 至 150,000 人。
- 招聘顾问：50 至 100 人。
- 模型版本：至少 3 个。

### 6.2 核心表

#### `dim_candidate`

| 字段 | 说明 |
|---|---|
| candidate_id | 匿名候选人 ID |
| experience_years | 工作年限 |
| education_level | 学历层级 |
| city_tier | 城市等级 |
| skill_cluster | 技能簇 |
| current_status | 当前求职状态 |

#### `dim_job`

| 字段 | 说明 |
|---|---|
| job_id | 岗位 ID |
| job_category | 岗位类别 |
| job_level | 岗位等级 |
| region | 招聘地区 |
| salary_min | 最低薪资 |
| salary_max | 最高薪资 |
| required_experience | 要求经验 |
| difficulty_level | 招聘难度 |

#### `dim_recruiter`

| 字段 | 说明 |
|---|---|
| recruiter_id | 顾问 ID |
| team | 所属团队 |
| region | 所属地区 |
| seniority | 顾问资历 |

#### `dim_model_version`

| 字段 | 说明 |
|---|---|
| model_version | 模型版本 |
| release_at | 上线时间 |
| retired_at | 下线时间 |
| description | 版本说明 |

#### `fact_recommendation`

| 字段 | 说明 |
|---|---|
| recommendation_id | 推荐 ID |
| recommended_at | 推荐时间 |
| candidate_id | 候选人 ID |
| job_id | 岗位 ID |
| recruiter_id | 顾问 ID |
| source | `ai` 或 `human` |
| model_version | AI 模型版本，人工推荐为空 |
| recommendation_score | 推荐分数 |
| rank_position | 推荐排名 |

#### `fact_funnel_event`

| 字段 | 说明 |
|---|---|
| event_id | 事件 ID |
| recommendation_id | 推荐 ID |
| event_type | contact/reply/interview/offer/hire |
| event_at | 事件时间 |
| event_status | 成功、失败或取消 |
| failure_reason | 失败原因 |

### 6.3 数据分层

```text
raw_*       数据生成器产生的原始记录
dim_*/fact_* 清洗后的维度表和事实表
mart_*      面向 Dashboard 的指标结果表
```

核心数据集市：

- `mart_daily_funnel`
- `mart_cohort_conversion`
- `mart_ai_effectiveness`
- `mart_feature_drift`
- `mart_monitoring_alert`

### 6.4 合成数据场景

数据生成器必须使用固定随机种子，并植入以下可验证场景：

1. AI 更容易被分配给经验丰富的候选人，产生选择偏差。
2. 模型 V2 在销售岗位的真实面试概率下降。
3. 华东某顾问团队在一个月内首次联系明显变慢。
4. 模型 V3 上线后候选人经验和推荐分数发生漂移。
5. 最近 30 至 90 天的 Offer 和入职结果尚未完全成熟。

每个场景应在生成配置中明确记录预期方向，自动测试需要验证分析模块能够识别这些方向。

### 6.5 外部数据与爬虫边界

- 核心招聘漏斗事件使用合成数据，或未来接入的企业授权脱敏 ATS 数据。
- 公开岗位、职业分类和官方统计数据只用于丰富岗位、技能、行业、地区和薪资分布。
- 爬虫不得采集候选人姓名、联系方式、原始简历、账号内数据及其他个人信息。
- 不绕过登录、验证码、付费权限、访问频率限制或其他技术保护措施。
- 采集前必须检查数据来源的服务条款、robots 规则、授权方式和再发布限制。
- 所有外部数据必须记录来源 URL、采集时间、批次、内容哈希和解析器版本。
- 真实公开数据与合成数据必须通过 `data_origin` 字段和物理表分开，不混合宣称为真实招聘效果。
- GitHub 默认不提交批量抓取的原始内容，只提交采集代码、字段说明和许可公开的小样本。

完整的数据流、表结构和执行顺序见 `数据流设计.md`。

## 7. 功能需求

### 7.1 总览页面

- 展示推荐量、AI 占比、联系率、30 天合格面试率、Offer 率和成熟队列入职率。
- 展示最近 12 个月核心指标趋势。
- 对比 AI 与人工推荐的原始差异和调整后差异。
- 展示当前未解决告警及严重程度。
- 支持日期、岗位、地区、模型版本和推荐来源筛选。

### 7.2 效果评估页面

- 展示 AI 与人工推荐漏斗对比。
- 展示不同岗位、地区和候选人层级的效果差异。
- 展示效果值、95% 置信区间和样本量。
- 展示调整前后的标准化均值差异（SMD）。
- 使用倾向得分加权或匹配降低可观测选择偏差。
- 在页面显著位置说明观察性分析的限制。

### 7.3 漏斗与队列页面

- 展示招聘漏斗各阶段数量、阶段转化率和累计转化率。
- 支持从总体下钻到岗位、地区、顾问团队。
- 展示按推荐月份划分的队列热力图。
- 标记尚未达到观察窗口的未成熟队列。
- 展示各环节耗时分布和超时比例。

### 7.4 模型监控页面

- 展示模型版本流量和效果趋势。
- 对数值特征计算 PSI，对类别分布计算 Jensen-Shannon divergence。
- 展示基准期和当前期的分布对比。
- 展示指标漂移的开始时间、影响范围和严重程度。
- 支持按模型版本、岗位和地区下钻。

### 7.5 异常诊断页面

- 对比异常期和基准期的漏斗指标。
- 将总体变化拆解到岗位、地区、顾问团队和模型版本。
- 初步分类为数据问题、流量结构变化、模型问题、顾问操作问题或招聘流程问题。
- 输出包含证据指标的解释摘要。
- LLM 功能仅接收已经计算好的结构化指标，不允许自行计算或修改数字。

### 7.6 数据质量页面

- 数据更新时间和各表记录数。
- 主键重复率、关键字段缺失率。
- 孤立事件和非法事件顺序。
- 未来时间、负耗时和异常枚举值。
- 数据延迟与成熟度状态。

## 8. 分析方法需求

### 8.1 基础统计

- 描述性统计和分组聚合。
- 比例差与 95% 置信区间。
- 两比例检验、卡方检验和适当的非参数检验。
- 多重分群分析应标记样本量不足问题。

### 8.2 调整后效果分析

- 使用推荐发生前已经存在的特征训练倾向得分模型。
- 禁止将联系、回复或面试等处理后变量放入倾向模型。
- 检查共同支持区域和极端权重。
- 使用 SMD 展示调整前后平衡性。
- 输出平均处理效应估计、置信区间和敏感性说明。
- 结果表述为“调整后的关联”或“估计效果”，不声称已经证明因果关系。

### 8.3 漂移检测

- 数值特征：分箱后计算 PSI，并记录每个箱的贡献。
- 类别特征：计算 Jensen-Shannon divergence。
- 业务指标：使用滚动基准、控制限或同比/环比规则识别异常。
- 阈值必须配置化，页面展示阈值和基准期。

### 8.4 根因分析

- 计算异常期相对基准期的指标差异。
- 使用“分群占比变化 + 分群内指标变化”拆解总体变化。
- 输出贡献最大的前 N 个维度组合。
- 每个结论必须包含数据时间、样本量、指标变化和对应维度。

## 9. 技术方案

### 9.1 MVP 技术栈

- Python 3.12+
- MySQL 8
- Pandas
- SciPy、statsmodels、scikit-learn
- Streamlit
- Plotly
- pytest
- Docker Compose

### 9.2 第二阶段技术栈

- FastAPI：提供统一指标和分析 API。
- Pydantic：请求和响应数据校验。
- SQLAlchemy：数据库访问。
- Alembic：数据库迁移。

### 9.3 暂不采用

- Django
- Redis
- Celery
- Kafka
- Kubernetes
- React/Vue

只有出现明确业务需求和可演示价值时，才增加上述组件。

## 10. 非功能需求

### 10.1 可复现性

- 相同随机种子应生成一致的数据结果。
- 项目提供 `.env.example`，不得提交真实密码。
- 使用一条 Docker Compose 命令启动数据库、API 和 Dashboard。
- README 提供从克隆仓库到打开页面的完整步骤。

### 10.2 性能

- 常用 Dashboard 查询在本地环境中目标响应时间不超过 3 秒。
- 重型统计分析预计算并写入 `mart_*` 表。
- Streamlit 使用缓存保存稳定查询结果。

### 10.3 可测试性

- 数据生成器测试固定随机种子和字段约束。
- SQL 指标测试分子不大于分母、比例位于 0 至 1。
- 漏斗事件测试时间顺序。
- SMD、PSI、JSD 使用小型确定性样本测试。
- 集成测试验证 Dashboard 所需数据集市可以成功生成。

### 10.4 可解释性

- 所有指标提供口径说明。
- 所有统计结果同时展示样本量和置信区间。
- 所有告警展示基准期、当前期、阈值和贡献维度。
- 所有自动摘要必须可回溯到结构化指标。

## 11. 页面与交互规范

- 默认进入总览页，不制作营销型 Landing Page。
- 左侧统一放置日期、岗位、地区、来源和模型版本筛选器。
- 页面顶部只展示 4 至 6 个最重要 KPI。
- 图表颜色固定：AI 与人工推荐在所有页面保持一致。
- 比率图必须能看到分子、分母和置信区间。
- 样本量不足时隐藏推断结果并显示原因。
- 未成熟队列使用明确状态标记，不与成熟队列混合比较。
- 图表标题表达业务结论或问题，避免只写字段名称。

## 12. API 需求（第二阶段）

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/api/v1/health` | 服务和数据库健康检查 |
| GET | `/api/v1/overview` | 总览指标 |
| GET | `/api/v1/funnel` | 漏斗指标 |
| GET | `/api/v1/cohorts` | 队列数据 |
| GET | `/api/v1/effectiveness` | 原始与调整后效果 |
| GET | `/api/v1/drift` | 漂移结果 |
| GET | `/api/v1/alerts` | 告警列表与详情 |
| POST | `/api/v1/analysis/refresh` | 刷新分析结果 |

所有查询接口必须支持必要筛选参数，并返回指标值、样本量、时间范围和口径版本。

## 13. MVP 验收标准

满足以下条件即认为 MVP 完成：

1. 可通过固定随机种子生成至少 100,000 条推荐记录。
2. 数据库中存在完整维度表、事实表和 5 张核心数据集市。
3. 总览、效果评估、漏斗队列、模型监控和数据质量页面均可访问。
4. AI 与人工推荐比较包含原始差异、调整后差异、样本量和置信区间。
5. 页面能够识别数据生成器植入的至少 4 类异常场景。
6. 未成熟队列不会被错误计入完整入职效果。
7. 核心数据和统计函数具有自动化测试。
8. `docker compose up --build` 可以启动项目。
9. README 包含业务背景、架构图、数据说明、方法限制、截图和运行步骤。
10. 仓库不包含真实个人数据、密码或大体积数据库文件。

## 14. 后续增强项

- 使用生存分析估计尚未完成招聘流程的候选人结果。
- 增加双重稳健估计和敏感性分析。
- 增加异常订阅和通知。
- 使用 LLM 生成带指标引用的周报草稿。
- 增加模型公平性审计，但敏感属性不得进入推荐逻辑。
- 在分析功能稳定后，视岗位方向决定是否替换为 React 前端。

## 15. 作品集交付物

- 可公开访问的 Dashboard 或完整演示视频。
- GitHub 源代码和一键运行说明。
- 数据字典与指标字典。
- 分析方法说明和局限性声明。
- 系统架构图和数据流程图。
- 自动化测试结果。
- 一份面向业务负责人的分析报告。
- 可放入简历的量化项目描述。
