# Triage Labels

Use the default triage label vocabulary:

- `needs-triage`
- `needs-info`
- `ready-for-agent`
- `ready-for-human`
- `wontfix`

The `/triage` skill should apply these labels when moving issues through the triage state machine.
